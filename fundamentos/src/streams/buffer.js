const buf = Buffer.from('Olá mundo!')

console.log(buf) // Hexadecimal cada caractere é representado um letra ou número;
console.log(buf.toString()) // Representação em String;
console.log(buf.toJSON()) // Representação em Decimal dentro do JSON;
